/*airthmatic operators
logical operators
binary airthmatic operators
conditional operators
Ternary operators
oject operators
*/

 String s = ""
 if ( s != null && s.length() > 0) {
    println("Found")
} else {
    println("Not Found")
}

//Ternary operators
//Making simpler
String m = ""
result = ( s != null && s.length() > 0 ) ? 'Ternary Found': 'Ternary not found'
println("$result")
